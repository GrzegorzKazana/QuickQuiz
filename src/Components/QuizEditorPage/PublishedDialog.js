import React from "react";
import styled from "styled-components";
import Modal from "../Common/Modal";

const PublishedDialog = ({ open, ...props }) => <Modal open={open}>a</Modal>;
export default PublishedDialog;
